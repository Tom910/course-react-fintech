import React, { Component } from 'react';
import Button from '../components/Button/Button';

export default class NewLinks extends Component {
  render() {
    return (
      <div>
        <Button onClick={() => {}} />
        <Button styles={{ margin: '10px' }}/>
      </div>
    );
  }
}
